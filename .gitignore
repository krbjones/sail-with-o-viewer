# Sailties export copies (duplicates of the main tracks)
sailties_export/

# GeoTIFF chart files — large binaries excluded by default
*.tif
*.tiff
# ...except the bundled chart
!lac_deschennes_chart_1550_cog.tif

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini

# Local Claude Code settings
.claude/settings.local.json

# Python bytecode (serve.py imports build_tracks during testing)
__pycache__/
